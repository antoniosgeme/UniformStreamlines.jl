var documenterSearchIndex = {"docs":
[{"category":"section","location":"95-reference/#reference","page":"Reference","text":"","title":"Reference"},{"category":"section","location":"95-reference/#Contents","page":"Reference","text":"Pages = [\"95-reference.md\"]","title":"Contents"},{"category":"section","location":"95-reference/#Index","page":"Reference","text":"Pages = [\"95-reference.md\"]","title":"Index"},{"category":"type","location":"95-reference/#UniformStreamlines.ArrowData","page":"Reference","text":"ArrowData{D}\n\nHolds arrow glyphs computed by streamarrows.\n\nFields\n\npoints  :: Matrix{Float64} — D × N matrix of arrow base positions.\nvectors :: Matrix{Float64} — D × N matrix of arrow direction vectors (unit tangent scaled by scale).\nspeeds  :: Vector{Float64} — speed ‖field(p)‖ at each arrow, suitable for color-mapping.\nindices :: Vector{Int}     — column indices into the original StreamlineData.paths matrix, so per-vertex color arrays can be subsampled.\n\n\n\n\n\n","title":"UniformStreamlines.ArrowData"},{"category":"type","location":"95-reference/#UniformStreamlines.StreamlineData","page":"Reference","text":"StreamlineData{D}\n\nHolds the output of evenstream. D is the spatial dimension.\n\nFields\n\npaths  :: Matrix{Float64} — D × N matrix. Streamlines are stored column-by-column and separated by columns of NaN. Pass paths[1,:] and paths[2,:] (and paths[3,:] in 3-D) directly to a plotting library.\nlower  :: Vector{Float64} — lower corner of the domain box.\nupper  :: Vector{Float64} — upper corner of the domain box.\nfield  :: Function        — the interpolated velocity function field(p::AbstractVector) -> Vector{Float64}. Useful for querying velocities at arbitrary points, and required internally by colorize and streamarrows.\n\n\n\n\n\n","title":"UniformStreamlines.StreamlineData"},{"category":"method","location":"95-reference/#UniformStreamlines.box_intersection!-NTuple{5, AbstractVector}","page":"Reference","text":"box_intersection!(out, pin, pout, lower, upper)\n\nCompute the point where the segment pin → pout crosses the axis-aligned box [lower, upper] and write the result into out. Assumes pin is inside the box and pout is outside.\n\n\n\n\n\n","title":"UniformStreamlines.box_intersection!"},{"category":"method","location":"95-reference/#UniformStreamlines.colorize-Tuple{StreamlineData, Any}","page":"Reference","text":"colorize(data::StreamlineData, f) -> Vector{Float64}\n\nCompute a per-point scalar value along the streamlines for color-mapping. Returns a Vector{Float64} of length size(data.paths, 2) with NaN at separator columns.\n\nThe color function f\n\nf must have the signature\n\nf(pos::AbstractVector, vel::AbstractVector) -> Real\n\nwhere pos and vel are both length-D vectors.\n\nBuilt-in symbols (shortcuts):\n\nSymbol Equivalent function\n:norm/:speed (p, v) -> norm(v)\n:vx / :u (p, v) -> v[1]\n:vy / :v (p, v) -> v[2]\n:vz / :w (p, v) -> v[3]  (3-D only)\n:x (p, v) -> p[1]\n:y (p, v) -> p[2]\n:z (p, v) -> p[3]  (3-D only)\n\nExamples\n\ncolors = colorize(result, :norm)\ncolors = colorize(result, (p, v) -> p[1]^2 + p[2]^2)   # distance² from origin\ncolors = colorize(result, (p, v) -> v[1] / norm(v))     # cos(angle) with x-axis\n\n\n\n\n\n","title":"UniformStreamlines.colorize"},{"category":"method","location":"95-reference/#UniformStreamlines.evenstream-Union{Tuple{D}, Tuple{NTuple{D, AbstractVector}, NTuple{D, Function}}} where D","page":"Reference","text":"evenstream(axs::NTuple{D,AbstractVector}, fns::NTuple{D,Function};       kwargs...) -> StreamlineData{D}\nevenstream(axs::NTuple{D,AbstractVector}, arrs::NTuple{D,AbstractArray}; kwargs...) -> StreamlineData{D}\nevenstream(xs, ys,     ufn, vfn;          kwargs...) -> StreamlineData{2}\nevenstream(xs, ys,     U, V;              kwargs...) -> StreamlineData{2}\nevenstream(xs, ys, zs, ufn, vfn, wfn;    kwargs...) -> StreamlineData{3}\nevenstream(xs, ys, zs, U, V, W;          kwargs...) -> StreamlineData{3}\n\nCompute evenly-spaced streamlines via the Jobard–Lefer algorithm.\n\nInput styles:\n\nFunctions — velocity components are evaluated on demand; no interpolation overhead.\nArrays — pre-computed grids, linearly interpolated at integration points. Convention: U[i, j, ...] is the first velocity component at the point (axs[1][i], axs[2][j], ...), created from the comprehension U = [u(x, y) for x in xs, y in ys].\n\nThe 2-D/3-D flat forms are convenience wrappers around the N-D tuple form.\n\nKeyword arguments\n\nKeyword Default Description\nmin_density 4 Seeding grid density (domain divided into 10 × min_density cells/axis). Higher → more seed candidates → denser coverage.\nmax_density 10 Collision grid density (domain divided into 10 × max_density cells/axis). Higher → streamlines may pass closer together.\nseeds nothing Explicit seed points (a tuple/vector of D-vectors); overrides density grids.\nmin_length 2 Discard streamlines with fewer than this many vertices.\nallow_collisions false If true, streamlines pass through each other instead of being truncated.\nstepsize adaptive Integration step size. By default set to min(norm(domain) / (10 × max_density × 10), 0.05).\n\nReturns\n\nA StreamlineData{D}. Pass to colorize and streamarrows.\n\nExamples\n\nxs = LinRange(-2, 2, 200);  ys = LinRange(-2, 2, 200)\n\n# From functions (flat 2-D form)\nresult = evenstream(xs, ys, (x,y) -> -y, (x,y) -> x)\n\n# From pre-computed grids (flat 2-D form)\nU = [-y for x in xs, y in ys];  V = [x for x in xs, y in ys]\nresult = evenstream(xs, ys, U, V)\n\n# N-D tuple form (any dimension)\nzs = ts = LinRange(-2, 2, 200)\nresult = evenstream((xs, ys, zs, ts), ((x,y,z,t) -> -y, (x,y,z,t) -> x, (x,y,z,t) -> z, (x,y,z,t) -> t))\n\ncolors = colorize(result, :norm)\narrows = streamarrows(result; every=15)\n\n\n\n\n\n","title":"UniformStreamlines.evenstream"},{"category":"method","location":"95-reference/#UniformStreamlines.stream-Union{Tuple{F}, Tuple{D}, Tuple{Val{D}, Vector{<:Real}, Vector{<:Real}, F}} where {D, F}","page":"Reference","text":"stream(Val(D), lower, upper, u; kwargs...) -> Matrix{Float64}\n\nLow-level core: compute evenly-spaced streamlines of the vector field u over the axis-aligned box [lower, upper]. Works in any dimension D (D=2 or D=3 are typical).\n\nu must be a function u(p::AbstractVector) -> AbstractVector returning the velocity at position p.\n\nReturns\n\nA D × N matrix. Individual streamlines are concatenated column-by-column, separated by columns of NaN.\n\nKeyword arguments\n\nKeyword Default Description\nmin_density 4 Seeding grid density (domain divided into 10 × min_density cells/axis). Higher → more seed candidates → denser coverage.\nmax_density 10 Collision grid density (domain divided into 10 × max_density cells/axis). Higher → streamlines may pass closer together.\nallow_collisions false If true, streamlines pass through each other instead of being truncated.\nseeds nothing Explicit seed points; overrides density grids.\nmin_length 2 Discard streamlines with fewer than this many vertices.\nstepsize adaptive Integration step size. By default set to min(norm(domain) / (10 × max_density × 10), 0.05).\n\n\n\n\n\n","title":"UniformStreamlines.stream"},{"category":"method","location":"95-reference/#UniformStreamlines.streamarrows-Union{Tuple{StreamlineData{D}}, Tuple{D}} where D","page":"Reference","text":"streamarrows(data::StreamlineData{D}; every=10, spacing=nothing, scale=1.0) -> ArrowData{D}\n\nExtract arrow glyphs from a StreamlineData result.\n\nArrows can be placed in two modes:\n\nVertex mode (default) — an arrow every every path vertices. Fast, but non-uniform when vertex spacing varies.\nArc-length mode — an arrow every spacing units of arc length. Set spacing to a positive number to enable uniform placement. When spacing is given, every is ignored.\n\nKeyword arguments\n\nevery    :: Int   = 10      — (vertex mode) place an arrow every this many path vertices.\nspacing  :: Real  = nothing — (arc-length mode) place an arrow every this many units of arc length along each streamline. Overrides every when set.\nscale    :: Real  = 1.0     — length factor applied to each unit-tangent arrow vector; pass this directly to your quiver/arrow plotting call.\n\nReturns\n\nAn ArrowData{D} with fields:\n\npoints  — D × N base positions.\nvectors — D × N arrow vectors (unit tangent × scale).\nspeeds  — N-vector of ‖velocity‖ at each arrow, for color-mapping.\nindices — column indices into the original StreamlineData.paths matrix (nearest vertex to each arrow position).\n\nExamples\n\n# Vertex-based (legacy)\narrows = streamarrows(result; every=15, scale=0.05)\n\n# Uniform arc-length spacing\narrows = streamarrows(result; spacing=0.3, scale=0.05)\n\n\n\n\n\n","title":"UniformStreamlines.streamarrows"},{"category":"function","location":"95-reference/#UniformStreamlines.streamlines","page":"Reference","text":"streamlines(str::StreamlineData; kwargs...)\nstreamlines!(ax, str::StreamlineData; kwargs...)\n\nPlot evenly-spaced streamlines from precomputed StreamlineData.\n\nstreamlines creates a new figure; streamlines! adds to an existing axis or plot. Both work with Makie (via MakieExt) and Plots.jl (via PlotsExt).\n\nCommon keyword arguments\n\nKeyword Default Description\nwith_arrows false Draw directional arrowheads along streamlines\narrows_spacing automatic Arc-length spacing between arrows (uniform placement)\narrows_every nothing Legacy: place an arrow every N vertices; overrides arrows_spacing\n\nMakie-specific keyword arguments\n\nKeyword Default Description\ncolor :blue A color, or a per-point Vector from colorize\ncolormap — Colormap when color is numeric (e.g. :viridis, :plasma)\ncolorrange automatic (min, max) for the color mapping\nlinewidth inherited Streamline width\nmarkersize inherited (2-D) Arrowhead size. In 2-D this is in pixels; in 3-D it is in data units (default auto-scaled to ~2 % of domain diagonal)\n\nPlots.jl-specific keyword arguments\n\nKeyword Default Description\nline_z — Per-point color values from colorize\narrow_scale 1.0 Multiplicative scale factor for arrowhead size\ncolor :blue Series color / colormap when using line_z\n\nExamples\n\nusing UniformStreamlines\n\nxs = LinRange(-2, 2, 200)\nys = LinRange(-2, 2, 200)\nstr = evenstream(xs, ys, (x, y) -> -y, (x, y) -> x)\n\n# Makie\nusing CairoMakie\nstreamlines(str; color=colorize(str, :norm), colormap=:viridis,\n            with_arrows=true)\n\n# Plots.jl\nusing Plots\nc = colorize(str, :norm)\nstreamlines(str; line_z=c, color=:viridis,\n            with_arrows=true)\n\n\n\n\n\n","title":"UniformStreamlines.streamlines"},{"category":"method","location":"95-reference/#UniformStreamlines.trace!-Union{Tuple{F}, Tuple{Matrix{Float64}, Vector{Float64}, Vector{Float64}, AbstractVector, F, Float64, Int64, AbstractVector, AbstractVector, Int64}} where F","page":"Reference","text":"trace!(verts, pos, x, x0, u, stepsize, maxvert, lower, upper, sgn) -> Int\n\nIntegrate a single streamline from x0 into the pre-allocated buffer verts (D × maxvert) using midpoint (RK2) steps. pos and x are D-length working vectors. Returns the number of columns written.\n\n\n\n\n\n","title":"UniformStreamlines.trace!"},{"category":"section","location":"#UniformStreamlines.jl","page":"UniformStreamlines.jl","text":"Evenly-spaced streamlines for 2-D, 3-D, and N-D vector fields in Julia.\n\nUniformStreamlines.jl implements the Jobard–Lefer algorithm to produce streamlines that are uniformly distributed across a domain. It works with function-defined or grid-defined velocity fields and supports Plots.jl and Makie.jl for visualization.","title":"UniformStreamlines.jl"},{"category":"section","location":"#Installation","page":"UniformStreamlines.jl","text":"using Pkg\nPkg.add(\"UniformStreamlines\")","title":"Installation"},{"category":"section","location":"#Quick-Start","page":"UniformStreamlines.jl","text":"using UniformStreamlines\n\nxs = LinRange(-2, 2, 200)\nys = LinRange(-2, 2, 200)\n\nstr = evenstream(xs, ys, (x, y) -> -y, (x, y) -> 1 + x - y^2)\n\nPlot with Plots.jl:\n\nusing Plots\nstreamlines(str)\n\nOr with Makie:\n\nusing CairoMakie\nstreamlines(str)\n\n(Image: Quick Start)","title":"Quick Start"},{"category":"section","location":"#Features","page":"UniformStreamlines.jl","text":"","title":"Features"},{"category":"section","location":"#Function-or-Matrix-Input","page":"UniformStreamlines.jl","text":"Pass velocity components as functions or pre-computed arrays:\n\n# Functions\nstr = evenstream(xs, ys, (x, y) -> -y, (x, y) -> x)\n\n# Matrices (U[i,j] is the x-velocity at (xs[i], ys[j]))\nU = [-y for x in xs, y in ys]\nV = [ x for x in xs, y in ys]\nstr = evenstream(xs, ys, U, V)","title":"Function or Matrix Input"},{"category":"section","location":"#Density-Control","page":"UniformStreamlines.jl","text":"Adjust min_density and max_density to control how tightly streamlines are packed:\n\nxs = LinRange(-3, 3, 200)\nys = LinRange(-3, 3, 200)\n\n# Sparse\nstr_sparse = evenstream(xs, ys, (x, y) -> -1 - x^2 + y, (x, y) -> 1 + x - y^2;\n                    min_density=2, max_density=4)\n\n# Dense\nstr_dense = evenstream(xs, ys, (x, y) -> -1 - x^2 + y, (x, y) -> 1 + x - y^2;\n                   min_density=5, max_density=15)\n\nBoth parameters are unitless multipliers that scale an internal base grid of 10 cells per axis:\n\nmin_density (default 4) — Controls the seeding grid. The domain is divided into 10 × min_density cells per axis. One candidate seed point is placed per cell, so a higher value means more candidate starting points and denser coverage.\nmax_density (default 10) — Controls the collision-detection grid. The domain is divided into 10 × max_density cells per axis. When a streamline is being integrated, it checks this finer grid to decide whether it is too close to an existing streamline. A higher value allows streamlines to pass closer together before being truncated.\n\nThe ratio max_density / min_density determines how much room there is between the minimum spacing (set by the collision grid) and the seeding spacing. Typical values:\n\nStyle min_density max_density\nSparse 2 4\nNormal 4 (default) 10 (default)\nDense 5–8 15–30\n\n(Image: Density Control)","title":"Density Control"},{"category":"section","location":"#Coloring","page":"UniformStreamlines.jl","text":"Use colorize to compute a per-point scalar for color-mapping:\n\nstr = evenstream(xs, ys, (x, y) -> sin(π*x) * cos(π*y), (x, y) -> 0.2y)\nc = colorize(str, :norm)\n\nBuilt-in color symbols: :norm (or :speed), :vx (or :u), :vy (or :v), :vz (or :w), :x, :y, :z.\n\nYou can also pass a custom function (position, velocity) -> scalar:\n\nc = colorize(str, (p, v) -> p[1]^2 + p[2]^2)   # distance² from origin\n\nWith Plots.jl, pass the color array via line_z:\n\nusing Plots\nstreamlines(str; line_z=c, color=:viridis)\n\n(Image: Coloring by Speed)","title":"Coloring"},{"category":"section","location":"#Arrows","page":"UniformStreamlines.jl","text":"Add directional arrows along streamlines with with_arrows=true. Arrows are placed uniformly along the arc length of each streamline by default, so spacing is consistent regardless of how densely the path is sampled:\n\n# Makie — uniform arrows with automatic spacing\nstreamlines(str; with_arrows=true)\n\n# Plots.jl\nstreamlines(str; with_arrows=true)\n\n(Image: Arrows — Saddle Field)\n\nYou can control the arc-length distance between arrows with arrows_spacing:\n\n# Tighter arrow spacing\nstreamlines(str; with_arrows=true, arrows_spacing=0.15)\n\n# Wider arrow spacing\nstreamlines(str; with_arrows=true, arrows_spacing=0.5)\n\nAlternatively, use arrows_every to place an arrow every N-th path vertex. This is faster but produces non-uniform spacing when vertex density varies:\n\n# Vertex-based placement (non-uniform)\nstreamlines(str; with_arrows=true, arrows_every=20)\n\nControl arrow size with markersize (Makie) or arrow_scale (Plots.jl):\n\n# Makie — small vs large arrows\nstreamlines(str; with_arrows=true, markersize=8)   # small\nstreamlines(str; with_arrows=true, markersize=20)  # large\n\n# Plots.jl\nstreamlines(str; with_arrows=true, arrow_scale=0.5)  # half size\nstreamlines(str; with_arrows=true, arrow_scale=2.0)  # double size\n\n(Image: Arrow Size Comparison)","title":"Arrows"},{"category":"section","location":"#NaN-Masking","page":"UniformStreamlines.jl","text":"Return NaN from velocity functions to mask out regions of the domain. Streamlines will not enter or cross masked areas:\n\nu(x, y) = (x+1)^2 + y^2 < 1 ? NaN : x + y\nv(x, y) = (x+1)^2 + y^2 < 1 ? NaN : x - y\n\nstr = evenstream(xs, ys, u, v)\n\n(Image: NaN Masking — Circular Obstacle)","title":"NaN Masking"},{"category":"section","location":"#Seed-Points","page":"UniformStreamlines.jl","text":"Provide explicit seed points to control where streamlines originate:\n\nseed_x = [-1.0, 0.0, 1.0]\nseed_y = [ 0.0, 0.0, 0.0]\nstr = evenstream(xs, ys, (x, y) -> x + y, (x, y) -> x - y; seeds=(seed_x, seed_y))\n\n(Image: Seed Points)","title":"Seed Points"},{"category":"section","location":"#Unbroken-Streamlines","page":"UniformStreamlines.jl","text":"By default, streamlines are truncated when they approach an existing streamline. Set allow_collisions=true to let them pass through each other:\n\nstr = evenstream(xs, ys, (x, y) -> -y / (x^2 + y^2 + 0.1),\n                     (x, y) ->  x / (x^2 + y^2 + 0.1);\n             allow_collisions=true)\n\n(Image: Unbroken Streamlines)","title":"Unbroken Streamlines"},{"category":"section","location":"#3-D-Streamlines","page":"UniformStreamlines.jl","text":"The same interface extends to three dimensions:\n\nxs = LinRange(-2, 2, 50)\nys = LinRange(-2, 2, 50)\nzs = LinRange(-2, 2, 50)\n\nstr3 = evenstream(xs, ys, zs,\n              (x, y, z) -> -y,\n              (x, y, z) ->  x,\n              (x, y, z) ->  0.3z)\n\nA more interesting example — the Arnold–Beltrami–Childress (ABC) flow with directional arrows:\n\nA, B, C = 1.0, √2, √3\nstr3 = evenstream(xs, ys, zs,\n              (x, y, z) -> A * sin(z) + C * cos(y),\n              (x, y, z) -> B * sin(x) + A * cos(z),\n              (x, y, z) -> C * sin(y) + B * cos(x);\n              min_density=2, max_density=4)\nc3 = colorize(str3, :norm)\n\nusing GLMakie\nstreamlines(str3; color=c3, colormap=:magma,\n            with_arrows=true, markersize=0.12)\n\n(Image: 3-D ABC Flow)","title":"3-D Streamlines"},{"category":"section","location":"#N-D-Streamlines","page":"UniformStreamlines.jl","text":"For arbitrary dimensions, use the tuple form:\n\naxs = (LinRange(-2, 2, 50), LinRange(-2, 2, 50), LinRange(-2, 2, 50), LinRange(-2, 2, 50))\nfns = ((x, y, z, t) -> -y, (x, y, z, t) -> x, (x, y, z, t) -> z, (x, y, z, t) -> -t)\nstr4 = evenstream(axs, fns)","title":"N-D Streamlines"},{"category":"section","location":"#Calling-Conventions","page":"UniformStreamlines.jl","text":"evenstream supports two equivalent calling styles:\n\nFlat form — pass axes and velocity components as separate positional arguments. This is the most convenient syntax for 2-D and 3-D fields:\n\n# 2-D with functions\nstr = evenstream(xs, ys, (x, y) -> -y, (x, y) -> x)\n\n# 2-D with matrices\nstr = evenstream(xs, ys, U, V)\n\n# 3-D with functions\nstr = evenstream(xs, ys, zs, (x,y,z) -> -y, (x,y,z) -> x, (x,y,z) -> 0.3z)\n\n# 3-D with matrices\nstr = evenstream(xs, ys, zs, U, V, W)\n\nTuple form — pass axes as a tuple and velocity components as a tuple. This is the general N-D interface, but works in any dimension:\n\n# 2-D (tuple form)\nstr = evenstream((xs, ys), ((x,y) -> -y, (x,y) -> x))\n\n# 3-D (tuple form)\nstr = evenstream((xs, ys, zs), ((x,y,z) -> -y, (x,y,z) -> x, (x,y,z) -> 0.3z))\n\n# 4-D\nstr = evenstream((xs, ys, zs, ts), (f1, f2, f3, f4))\n\n# With pre-computed arrays\nstr = evenstream((xs, ys), (U, V))\n\nBoth forms accept the same keyword arguments (min_density, max_density, seeds, allow_collisions, etc.). The flat form is simply a convenience wrapper that forwards to the tuple form internally.","title":"Calling Conventions"},{"category":"section","location":"#API-Summary","page":"UniformStreamlines.jl","text":"Function Description\nevenstream Compute evenly-spaced streamlines\ncolorize Compute per-point color values\nstreamarrows Extract arrow glyphs for visualization\nstreamlines / streamlines! Plot recipe (Plots.jl or Makie)","title":"API Summary"},{"category":"section","location":"#Keyword-arguments-to-evenstream","page":"UniformStreamlines.jl","text":"Keyword Default Description\nmin_density 4 Seeding grid density (10 × min_density cells/axis). Higher → more seed candidates → denser coverage.\nmax_density 10 Collision grid density (10 × max_density cells/axis). Higher → streamlines may pass closer together.\nseeds nothing Explicit seed points (tuple/vector of D-vectors)\nmin_length 2 Discard streamlines with fewer than this many vertices\nallow_collisions false Allow streamlines to cross each other\nstepsize adaptive Integration step size; defaults to min(norm(domain) / (10 × max_density × 10), 0.05)","title":"Keyword arguments to evenstream"},{"category":"section","location":"#Keyword-arguments-for-Plots.jl-recipe","page":"UniformStreamlines.jl","text":"Keyword Default Description\nwith_arrows false Show directional arrowheads\narrows_spacing automatic Arc-length spacing between arrows (uniform placement)\narrows_every nothing Legacy: place an arrow every N vertices; overrides arrows_spacing\narrow_scale 1.0 Scale factor for arrow size\nline_z — Per-point color values from colorize","title":"Keyword arguments for Plots.jl recipe"},{"category":"section","location":"#Keyword-arguments-for-Makie-recipe","page":"UniformStreamlines.jl","text":"Keyword Default Description\nwith_arrows false Show directional arrowheads\narrows_spacing automatic Arc-length spacing between arrows (uniform placement)\narrows_every nothing Legacy: place an arrow every N vertices; overrides arrows_spacing\nmarkersize 12 (2-D) / 0.08 (3-D) Size of arrowhead markers\ncolor :blue Line / arrowhead color or per-point vector from colorize\nlinewidth inherited Width of streamlines\ncolormap — Colormap for color-mapped data","title":"Keyword arguments for Makie recipe"}]
}
